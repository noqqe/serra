# https://taskfile.dev

version: '3'

tasks:
  build:
    cmds:
      - go build -ldflags "-X serra.version=`git describe --tags`"  -v serra.go
    sources:
      - "src/serra/**/*.go"
      - "serra.go"
    generates:
      - "./serra"

  default:
    cmds:
      - task: build

